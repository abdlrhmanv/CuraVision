# ── CuraVision Frontend — environment template ────────────────────────────────
# Copy to .env:  cp .env.example .env
# NEVER commit .env.
#
# Public URL the browser uses to reach the Express backend (not the AI service).

NEXT_PUBLIC_API_BASE_URL=http://localhost:3001
